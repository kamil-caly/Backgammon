namespace GameLogic
{
    public class Move
    {
        private BoardField[,] board = new BoardField[12, 12];
        public Move(BoardField[,] board)
        {
            this.board = board;
        }

        
    }
}
